#include <iostream>
using namespace std;

class myclass{
     int n,d;
   public:
     myclass(int i,int j){
        n=i;
        d=j;}
   void get(){ return i,j;}
   // friend int arkads(myclass ob);
};

int arkads(myclass ob){
//    if(!(ob.n% ob.d)) return 1;
  //  else return 0;
    ob.get()
}

int main(){
    myclass ob(9,4);
    if(arkads(ob)) cout << " 9 , 4 un tam katıdır." << arkads(ob);
    else  cout << " 9 4 ün tam katı degildir.";
 }

