#include <iostream>
using namespace std;

int abs (int n);
long abs (long n);
double abs (double n);

int main(){
    cout << "-10 un mutlak degeri: " << abs(-10) << "\n\n";
    cout << "-10L nin mutlak degeri: " << abs(-10L) << "\n\n";
    cout << "-10.10 un mutlak degeri: " << abs(-10.10) << "\n\n";
 return 0;
}

int abs (int n){
    cout << " tam sayılar icin abs()\n";
    return n<0 ? -n : n;
}
long abs (long n){
    cout << " long sayılar icin abs()\n";
    return n<0 ? -n : n; 
}
double abs (double n){
    cout << " double sayılar icin abs()\n";
    return n<0 ? -n : n;
    
}